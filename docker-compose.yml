services:
  astro:
    image: bennet-vella-site:latest
    build:
      context: .
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "4321:4321"
    command: npm run dev -- --host 0.0.0.0
