# Plans

This directory stores task plans that have been scanned and proposed but not yet implemented.

Each plan should be a standalone Markdown file with:

- objective
- current state
- target state
- files likely to change
- risks or assumptions
- validation approach

When implementation starts, move the file to `docs/impl/` and continue updating it there.
