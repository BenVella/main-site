# Done Docs

This directory contains task records that have already been implemented and carried out.

- Move task documents here from `docs/impl/` when implementation is complete.
- Keep files in Markdown format and human-readable.
- Use `docs/wiki/` for durable reference material that should outlive a single task record.
